version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: handmade-shop-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: handmade_shop
      POSTGRES_PASSWORD: handmade_shop_dev
      POSTGRES_DB: handmade_shop
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
